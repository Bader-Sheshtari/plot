import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "PLOT — AI Screenplay Generator",
  description:
    "Turn your movie idea into a full screenplay with AI. PLOT helps you transform one simple idea into a complete story, characters, scenes, and screenplay draft.",
  keywords: "screenplay, AI, movie, film, story generator, scriptwriting",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
